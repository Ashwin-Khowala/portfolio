const config = {
  plugins: {
    "@tailwindcss/postcss": {
      layers: {
        properties:{},
        theme: {},
        base: {},
        components: {},
        utilities: {},
      },
    },
  },
};

export default config;
